package streams;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class practice1 {
//
//     List<Employee> empList = new ArrayList<>();
//        empList.add(new Employee(1, "abc", 28, 123, "F", "HR", "Blore", 2020));
//        empList.add(new Employee(2, "xyz", 29, 120, "F", "HR", "Hyderabad", 2015));
//        empList.add(new Employee(3, "efg", 30, 115, "M", "HR", "Chennai", 2014));
//        empList.add(new Employee(4, "def", 32, 125, "F", "HR", "Chennai", 2013));

    // name , gender, age

    //  no of male and female count in the arraylist(obj)

   //  Map<String,Long> countGen= emp.stream().collect(Collectors.groupingBy(empl->empl.getGender(),Collectors.counting()))
    //sout(countGen);


    // emp.stream().max(Comparator.comparing(Employee::getAge)


    //Optional<Employee> maxAgeEmployee = emp.stream()
    //    .max(Comparator.comparing(Employee::getAge));

    List<String> list = Arrays.asList("PowerfulRhea","Powerkingkong","Pookiespiderman", "batman" ,"capptainamerica" ,"Prettyhulk");


    // find p

 //  list.stre


}
