package INTERFACE;

public class Reena implements exStatic{

    public void bestLaugh(){
        if(exStatic.canLaugh()){  // we can use the method like this by calling them there interface itself

        }
    }
}
